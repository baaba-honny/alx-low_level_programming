#include <stdio.h>

/**
*       main - this is the main function
*       Return: always zero
*/

	int main(void)
{
	char m;

	for (m = 'a'; m <= 'z'; m++)
	
	putchar(m);
	
	for (m = 'A'; m <= 'Z'; m++)
	
	putchar(m);
	putchar('\n');
	
	return (0);

}
